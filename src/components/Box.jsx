import styled from '@emotion/styled';
import {
  typography,
  color,
  space,
  layout,
  flexbox,
  border,
  shadow,
} from 'styled-system';

export const Box = styled('div')(
  color,
  typography,
  space,
  layout,
  flexbox,
  border,
  shadow
);
